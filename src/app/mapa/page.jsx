import MapaCompleto from "@/components/mapa-completo";

const page = () => {
  return (
    <section className=" max-h-[100dvh] overflow-hidden">
      <MapaCompleto />
    </section>
  );
};

export default page;
